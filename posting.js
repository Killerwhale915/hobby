function submitForm() {
    var name = document.getElementById("name").value;
    var email = document.getElementById("text").value;
    document.getElementById("log").innerHTML =
        "이름: " + name + "<br>글 내용 : " + text;
}
